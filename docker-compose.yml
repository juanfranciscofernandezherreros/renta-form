version: "3.9"

services:
  db:
    image: postgres:16-alpine
    container_name: renta_form_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: renta_form
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
      - ./database/schema_backend.sql:/docker-entrypoint-initdb.d/02-schema_backend.sql

volumes:
  postgres_data:
